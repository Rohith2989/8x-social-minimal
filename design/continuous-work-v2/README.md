# Continuous page refinement and local scroll sequence

User likes the continuous hero-to-real-content direction and requested greater hero polish and an image of the smaller scroll transitions. This iteration refines hierarchy, spacing and common accent colour. It remains a generated concept, not implemented or tested animation. Preserve actual source media and original SVG during implementation; generated mockup pixels are not production originals.

## Motion proposal

1. Hero photograph moves naturally with the document. No independent portrait motion, photo morph or circle takeover.
2. Creator content follows on the same carbon surface with a maximum12px entrance over300ms. Smaller heading supports rather than competes with the hero.
3. Brief bounded sticky reading interval (initial proposal roughly one viewport) holds copy and three video rectangles. Ordinary native scrolling drives selection. No intercepted wheel/touch events, timed delay or snapping.
4. Judy, then Jack, then product demo becomes active. Only caption, selector underline, brightness emphasis and progress dot change, about180–250ms. Video widths, aspect ratios, positions and overall frame remain fixed. This supersedes v1's local translation/scale proposal. Any generated frame misalignment is not intended choreography. Inactive dimming should be subtle, not obscure content.

Only active visible clip plays muted. Manual media/selector controls and keyboard access remain. Manual selection and user-enabled sound must not immediately be overwritten by scrolling; resolve those interaction details in implementation. Reduced motion and small screens should present readable normal flow and manual selection without extra scroll distance. Other states are shown by the storyboard rather than represented as newly implemented behaviour.

## Later page direction, recorded but deferred

Keep the existing Reach Atlas map and its orange colour. Actual current source colour is #f34b32 in the old project's app/reach-atlas.css. Use this same orange for small accents now; reserve the large background-colour change for the eventual map approach. User called the departing surface beige, but did not explicitly request reversing the liked carbon hero or restoring rejected stone. Current generated proposal therefore keeps carbon; exact future transition treatment remains to be designed.

After the map, user wants the 8 to become infinity and introduce the rest of the 8x products/family (Business, Sale and others to be confirmed from actual sources). User explicitly said this is not for now. Record the direction only; do not build/generate the finale, assume a full product list, invent offers or reinstate the former pressed-logo finale by default in the minimal repository.

01-refined-page.png and02-scroll-sequence.png are original built-in outputs. prompts.json preserves exact prompts/reference roles. Old follow-on diagrams/portrait repeats remain rejected. No application changes or Git push requested this turn.
