# Dot reach — next-section proposal

Replaces the rejected partner-quote concept. One creator image, one headline and three small platform marks on the continuous stone surface. A photographic raster dissolves only the image edges, making the dots the main brand treatment.

User approved this direction and implementation is now at /#reach. The generated image used Jack only as a concept reference. Per the user's subsequent request, the implementation instead uses original Maggie footage (different from every other live section), a code-based raster overlay, and existing platform SVG assets. The generated portrait is not shipped as the live UI. The studio hero montage is not repeated.

Proposed motion: during ordinary scroll into view, resolve the outer dot band into its final print-like edge. Keep the creator's face and text stable. Hover shifts only the narrow outer dot band; no whole-photo travel, global particles, scene wipe or automatic video audio. Use static dots for reduced motion. Keep the surface stone throughout. The later orange map remains deferred.

Exact original generation prompt and input roles are in prompts.json. QA captures: ../../docs/qa/dot-reach-desktop.png and dot-reach-mobile.png. The original concept image is retained as the approved design reference.
