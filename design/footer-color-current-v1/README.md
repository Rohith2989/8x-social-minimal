# Colour current — proposed dotted infinity motion

Update: the user selected this board. Subsequent broad rainbow and fast particle interpretations were rejected. The live implementation now uses a short, slow colour trace over the board's sampled dot arrangement, following the latest request for only a speck of colour and subtle movement. See ../../docs/FOOTER.md. The board remains an art-direction reference, not a claim of exact runtime fidelity; latest implementation awaits review.

The user found the monochrome living-infinity proposal too bland and explicitly requested vibrant colour streaming through the dots after conversion. This supersedes the monochrome-only constraint for the footer artwork, not the surrounding site palette.

`01-color-sequence.png` is a built-in generated storyboard. Exact prompt is saved in `prompt.json`. Orange/coral leads a magenta, violet, cobalt and cyan current around both loops and through the crossover. The underlying silhouette remains recognizable, with carbon intervals, subtle breathing and shallow depth. The page background, typography and product list stay stationary and neutral.

Four frames illustrate dots formed, current crossing, full flow and continuous circulation. Diagram arrows, title, labels and separating rule belong to the board only. This is a still concept, not a video or live implementation. Timing, displacement and performance must be verified in the live build if selected. Reduced motion and offscreen pause remain required. No live code changed for this proposal.
