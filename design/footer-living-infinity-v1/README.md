# Living infinity — motion concept

The user clarified that the dotted endpoint itself must stay alive: moving dots, breathing and layered depth. This supersedes the prior assumption that the final halftone should remain static. Preserve the approved stone footer, product directory and recognizable horizontal 8 silhouette.

`01-motion-sequence.png` is a built-in image-generation concept, not a runtime implementation or video. `prompt.json` records the exact prompt and reference. The generated board illustrates a six-second seamless cycle: left loop lifts, a crest passes through the crossover, right loop lifts, return. Dots circulate around both loops; local size, contrast and shallow layer offsets make the crest readable. The directory stays still. Arrows, timings, captions and board divider are explanatory annotations, not proposed site UI.

For implementation, retain recognizable negative spaces and avoid a scattered particle cloud. Aim for small bounded displacement, a subtle breathing envelope and smoothly connected loop motion. Actual timing and travel distances require live tuning; a still board cannot prove smooth motion. Pause offscreen/hidden and provide a static reduced-motion endpoint. The user asked to see the sequence first; live code remains at c797c0e in this turn.
