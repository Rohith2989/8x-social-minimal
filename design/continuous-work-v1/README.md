# Continuous hero-to-work concept

User correction: keep the carbon hero, but scrap all previous next-section designs. They still looked like standalone scenes. Hero studio portraits must appear only in the hero. Use actual creator assets/videos from the original website afterward. Whole page should have a continuous reading flow and restrained scroll-linked changes within fixed regions.

01-continuous-page.png deliberately shows the hero and creator work in ONE long scroll capture. No repeated navigation, new oversized headline, standalone network diagram, separate full-screen chapter, background wipe or repeated portrait subjects. Smaller heading and natural-colour real content sit directly beneath the hero on the same carbon surface. This is a built-in-generated layout visualization, not implemented output or exact pixel-preserving media compositing.

## Source assets

The original 8x for-brands page was checked this turn and lists Judy, Jack and the product demo under recent creator work. Exact original saved posters are preserved in references. Original playable files remain in the old 8x-social repository under public/creators/judy.mp4, jack.mp4 and network.mp4, with webm equivalents. prompt.json includes source links and exact generation prompt. Production must reuse those original files and poster pixels, not generated reconstructions of faces, subtitles or product UI.

## Proposed scroll behaviour

Natural scroll moves hero up; no portrait animation and no giant circle morph. The proof content follows immediately. Once the compact media stage reaches a comfortable reading position, a bounded sticky interval of roughly one viewport lets scroll progress select the three clips. Headline, paragraph and stage bounds stay still. Only the selected label/caption, fine orange progress indicator and relative media emphasis change. Keep each source video undistorted and uncropped; preserve full 9:16 images. Start with 24-40px local horizontal movement rather than a whole-screen horizontal carousel. The generated image's staggered top edges are visual emphasis, not instructions for vertical jumping.

One clip plays muted when active and visible; stop inactive/hidden clips. Playing with sound is user-initiated and should hold that selection until resumed or the stage leaves view. Provide real playback controls. Text selectors must also be clickable and keyboard accessible; manual selection should not be immediately undone by a pending scroll event. No wheel/touch interception, timed lock, forced snapping or auto cycling. Reduced motion/mobile use a normal vertically readable list or direct selectors without extra scroll distance. No claim of performance testing; this is still a concept.

Previous next sections are rejected history: network-v1, network-v2 and carbon-continuation-v1/02-next-section.png plus 03-transition-sequence.png. Retain archives rather than deleting them. The carbon hero body is liked; navigation proposals still need their own review. New continuation awaits user review. No code, commit or push this turn.
