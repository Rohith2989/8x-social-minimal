# Continuous reach — proposed map approach

REJECTED: user explicitly corrected the stone map and direct shortcut. Keep the original orange map and restore missing original-site content before it. Do not implement this composition or its map approach.

Built-in image generation, saved as `01-continuous-page.png`. Exact prompt: `prompts.json`. This is a concept for review, not an implemented map or an approved replacement for the map's orange finish.

Latest correction: stop designing separate slides. Drop the rejected product/chart section and use a direct, open continuation from the creator passage toward the original map. Shared margins, typography and dot material connect the content without rules, bars, cards or decorative particle trails. The hero montage is not repeated.

The image shows an early approach state on the common stone surface. Proposed motion: ordinary document scrolling, bounded movement of the existing portrait raster, then a gradual change of the shared surface toward the original orange as the map enters. No horizontal wipe, viewport takeover or added scroll lock. Reduced motion would use a stable orange map treatment. This motion and map integration are not built yet.

Use the existing original Reach Atlas geometry, density, dataset and interactions when implemented; the generated map is only a visual placement study, not geographic or coverage data. Do not copy generated country highlights. The real video reference is Maggie, used once. Do not ship this raster as the live UI.

Implemented separately in this turn: remove live section/nav/accordion rules; shorten the stone-to-stone spacing; let the sticky navigation match the stone surface once reached, reverting on upward scroll. Functional video controls, focus outlines and active navigation feedback remain.
