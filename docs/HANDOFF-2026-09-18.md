# Overnight checkpoint — September 18

Branch: `work/minimal-polish-2026-09-18`.
Base main: `abb82a37c4ed637a96e3ae1c09a5d3d4b04c24f0`.

The user explicitly requested saving all current work to a remote branch, without merging or automatically deploying. `vercel.json` disables Git deployments for this exact branch only. Main is untouched. If this file is later merged, its rule still only matches the working branch; it does not disable main. No PR, merge or deployment was requested. Further development stays local unless the user asks for another push.

## Current review

http://localhost:3904/?v=services-pinned-1#services

Next production server runs on 3091; 3904 is its local proxy. Do not stop unrelated servers on 3000, 3088 or 3090. Repository: `C:/Users/rohit/OneDrive/Documents/ChatGPT/haha/8x-social-minimal`.

## Saved work

- Breathing hero edge field and consistent halftone creator-video treatment with the quiet navbar.
- Combined showcase surface: 18 curated clips from 42 original-site videos imported locally; bounded active playback and a shared colour reveal.
- Editorial budget comparison with the approved woman image, always-visible comparisons and fluid black hover/focus/touch reveals.
- Partner feedback: automatic dotted 2.2M pressure ripple and central quote ink bloom, settling into readable still content.
- Orange Self Serve / Full Service section with generated transparent photographic hand sprite sheets and the approved single-hand conductor.
- Latest user-approved correction: service section pins during native scrolling. Self Serve scrubs first, then a pause, then Full Service, then a final hold before releasing into the original orange map. Desktop uses a 1.8-viewport scroll runway; mobile pins each choice separately. Reverse scrolling reverses the exposures. Reduced motion, short viewports and no-JavaScript avoid the added runway.
- Shared stone-to-orange approach starts before Services and continues through the unchanged original map. Scroll Thread contrast follows it.
- Design boards, source sheets, exact prompts, implementation docs and QA screenshots included.

## Validation

Latest production build passed. Service pin tests cover sequence, stationary geometry, no timer-driven advancement, reverse scrubbing, release to the map, CTA visibility at 320/390/700/768/1366/1920/2560px, reduced motion and no-JS. Map coverage/interactions and mobile fallback passed. One map readiness check timed out on the first suite run; its isolated rerun passed. Earlier partner-feedback and comparison motion checks passed. No full-suite claim: some earlier sections have intentionally changed since historical tests.

## Next session

Continue from this branch. The user said the pin is nice and requested this checkpoint; no further section redesign is currently specified. Preserve the services approval. A separate standalone infinity footer redesign was discussed earlier and remains future work; do not treat the current old 8-to-infinity entrance as newly approved. Read `design/CURRENT-DIRECTION.md`, `docs/SERVICE-CHOICES.md`, `docs/PARTNER-FEEDBACK.md`, `docs/COMPARISON.md` and `docs/VIDEO-SURFACE.md` for details.

Vercel branch exclusion reference: https://vercel.com/docs/project-configuration/git-configuration#git.deploymentenabled
