# September 18 release

The user requested all of today's and yesterday's implementation work on `main` so the connected Vercel project deploys it. This includes checkpoint `76cb637` and the subsequent local changes.

## Included

- Shared creator-media treatment, continuous showcase, editorial comparison and fluid ink interactions from the polish checkpoint.
- Partner feedback and photographic Service Choices; the latest Services behavior is a continuous eight-second clock independent of scrolling or section entry, with a reduced-motion still state.
- Comparison automatically reveals the 8x Network panel when entering the section, then accepts pointer, keyboard and touch selection.
- Three-stage dashboard before the map: overview, 20-tile content library, and creator performance. Includes real platform marks and clearly illustrative reporting data.
- Original headline and yellow panorama on carbon, photo aligned to the opening viewport bottom, original cursor-reactive raster sequence into five coloured portraits. The Many Voices artwork is removed.
- Responsive, keyboard, reduced-motion, no-JavaScript and media fallback refinements, supporting source assets, design references and QA captures.

## Deferred

The current implemented family/footer remains for this release. The user is exploring a replacement shared infinity/product showcase and explicitly deferred further work until after this push. Generated concept images do not constitute implementation approval. The fifth product label, 8x Next, remains a placeholder.

## Git and deployment

Merge the polish branch and release checkpoint into `main` without rewriting existing history. Vercel's checked-in Git deployment exclusion applies only to `work/minimal-polish-2026-09-18`; it does not disable `main`. Future changes remain local until the user requests another push.

## Validation

- Optimized Next.js production build and TypeScript validation passed.
- The full 44-test Playwright run passed 40 checks and identified four failures. Release fixes removed dashboard panel translation that triggered browser scrolling, bounded the dashboard dot field at tablet widths, and corrected two test assumptions (proportional edge coverage and lightweight frame sampling).
- Focused reruns verified all previously failing checks and the affected dashboard, Services, creator-media, navigation, and landing-page behavior. Final dashboard/Services run: 7 passed. The remaining checks passed in the full run or the preceding focused run; no known test failures remain.
- `git diff --check` passed. Hero photo positioning was verified across ten viewport sizes; the original cursor ripple, five-colour portrait reveal, reduced motion and fallback rendering passed.
