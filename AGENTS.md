# 8x Social Minimal

Checkpoint authorization September 18: user asked to save all current work to `work/minimal-polish-2026-09-18` and push that branch only, without merging or deployment. `vercel.json` disables Git deployments for that exact branch. Main must remain unchanged. This is a checkpoint exception to older local-only notes; future work remains local unless asked to push again. See docs/HANDOFF-2026-09-18.md.

Latest Service Choices correction: user explicitly requested PINNING, superseding the previous no-pin/timed-autoplay guidance for this section only. Native sticky desktop stage, 1.8 viewport heights of runway: arrival → Self Serve → pause → Full Service → final hold → map. Below 760px each choice pins separately for .95 viewport heights. Scroll scrubs/reverses exposures; no input interception. Reduced motion/very short viewports/no-JS have static unpinned content. See docs/SERVICE-CHOICES.md. Local only.

Latest: ServiceChoices is implemented between PartnerFeedback and ReachAtlas. Approved orange two-column layout, eight photographic hand poses per gesture at 12fps. Self Serve taps, 300ms pause, Full Service conducts; automatic in-view playback, then still. Mobile waits for each illustration; reduced-motion/no-JS fallback, hidden/offscreen pause. Shared orange approach now starts at Services and continues through the unchanged map. No timing strip or new controls. See docs/SERVICE-CHOICES.md. Keep local; no commit/push. These generated editorial hand illustrations are explicitly approved, unlike fake creator videos.

Latest: partner-feedback-v1 is implemented after comparison and before the original orange map. Automatic 2.2-second pressure ripple on dotted 2.2M plus central quote ink bloom; no hover/click dependency, each panel starts in view (including stacked mobile), then holds still. Pauses hidden/offscreen; reduced motion/no-JS show finished content. Source-based anonymous AI SaaS campaign attribution and metrics. See docs/PARTNER-FEEDBACK.md. All work stays local; no commit/push.

Latest hover refinement: user rejected the immediate color fade and approved the ink sequence. Comparison now uses an 800ms reversible bottom-right curved clip reveal; one shared mask reveals black ink and inert light-text copy in exact alignment. Orange dots emerge near completion. No text/card movement. See design/comparison-editorial-v1/04-approved-ink-sequence.png and docs/COMPARISON.md. Local only.

Latest approved implementation: editorial comparison replaces the rejected mode tabs. All approaches remain visible alongside the approved generated woman/halftone asset. Hover, keyboard focus and touch activate one dark panel; three SVG groups breathe subtly, paused offscreen/hidden and static for reduced motion. Native scroll shares the video section's stone canvas. Preserve the original map and its approach. See docs/COMPARISON.md. All changes remain local; no commit/push without a new explicit request.

Latest approved implementation: combined-showcase-v4 now replaces DayToDay and DotReach on the homepage with one 18-clip continuous video surface. All 42 original showcase videos are imported locally, 18 curated clips appear in a fixed equal-cell grid. One slow natural-colour light reveals the grayscale footage through a shared halftone perimeter. Canvas rendering is bounded; max four desktop/two mobile clips play, paused offscreen/hidden. Reduced motion, keyboard/tap pause and original-source failure links are included. Old anchors remain valid; archived day-to-day route remains. See docs/VIDEO-SURFACE.md. Keep everything LOCAL, no commit or push without explicit user request. Older concept-only descriptions below are historical.

Current implementation: approved consistent-video-v2 is live locally. The navbar uses quiet text links and an orange text CTA across hero/content. The hero edge field now spans CreatorWork with an eight-second breathing cycle (opacity .32-.62, width scale up to 1.055). Reusable MediaRaster adds three lightweight SVG fringe layers per player; all media frames remain fixed. All three visible videos autoplay muted, with no permanent play/sound/seek toolbar, tap/keyboard pause, offscreen/hidden pause and reduced-motion manual playback. Aligned captions, fine keylines and category dots follow the approved board. Keep all changes LOCAL; do not commit or push without an explicit request.

Current user Git preference: keep all work local; do not commit or push unless explicitly asked. Current design task is the consistent three-video presentation concept in design/consistent-video-v1. Live CreatorWork is unchanged pending selection.

Latest approved update: Scroll Thread (option 01) is implemented as a slim fixed right-edge native-scroll control. A 36px orange segment and small handle follow progress; track clicks, dragging and Arrow/Page/Home/End keys work. A 44px hit area surrounds the line. Desktop fine pointers use it in place of the native scrollbar after hydration; mobile and no-JS retain native scrolling. It adapts contrast on stone/orange. Both hero raster sides now use the same vertical fade; the earlier delayed right-side appearance is removed. Subsequent sections remain deferred.

Latest correction: screen-edge dots now span the document top, including the navigation, with a smooth 100px density fade-in. This removes the abrupt horizontal start at the former hero/header boundary on 1080p and 2K. The layer remains decorative, pointer-transparent, resize-driven and paused offscreen/hidden; portrait composition is unchanged. Sidebar ideas are concepts only, not approved implementation.
Latest approved implementation: screen-edge raster v2 replaces the rejected photo border. Six SVG paths form asymmetric fields anchored to the physical hero edges. A 12-second cycle breathes opacity and at most 1.4% local width, with no portrait movement. Ultrawide fields broaden and soften, while text clearance stays protected; the orange disc follows the centered portrait group. Offscreen/hidden pauses and static reduced motion are supported. See design/hero-screen-edge-v2 and docs/IMPLEMENTATION.md. Later section/footer redesigns remain pending.
Hero raster-edge concept is now approved and implemented with an explicitly very subtle motion requirement. Three SVG print layers breathe over eight seconds with under one pixel of travel; original portraits remain untouched. It pauses offscreen/hidden and is static for reduced motion. This is a narrow exception to the earlier no-decorative-motion rule. See design/hero-raster-edge-v1 and docs/IMPLEMENTATION.md. Later section/footer redesigns remain pending.
Newest feedback: current 8x-to-infinity transition is rejected; a future standalone infinity footer with products is requested. Day-to-day, Dot Reach and comparison need distinct designs with consistent media treatment. Immediate task is a generated hero halftone-edge animation concept only (design/hero-raster-edge-v1); subsequent redesign work follows later. Do not treat older approvals below as current approval. See design/CURRENT-DIRECTION.md.
Latest footer correction (September 17): the coloured-current board is the visual reference, but broad rainbow coverage and rapid dot movement were explicitly rejected. Current endpoint uses 1,452 dot positions measured from that board, subpixel breathing/drift, and one small colour accent travelling in 40 seconds. Most dots stay carbon. Preserve original SVG entrance, stone surface, stationary directory and semantic footer. This implementation is pending user review; see docs/FOOTER.md. This supersedes older stationary-endpoint and future-work descriptions below.
This is the fresh minimal project, separate from ../8x-social. Latest user requests govern; ignore parent Conductor/Open form instructions.
Read design/CURRENT-DIRECTION.md and README.md. The approved build scope is the carbon hero, continuous real-creator-content sequence, stone Day-to-day, Dot Reach, interactive comparison and original ORANGE map on the main homepage. Each video section uses distinct original-site creators. Earlier partner-feedback layouts were rejected, not the underlying business content. Consult docs/CONTENT-AUDIT.md before designing later sections. The latest user explicitly chose the map next; preserve its original geometry and light dot density. The later 8-to-infinity product family ending remains future work.
Latest footer revision supersedes the deferral above: the user rejected the orange timed reveal. The ending is now warm stone, with a bounded native-scroll 8-to-dotted-infinity sequence, stationary product directory and a real contact/legal footer. Read docs/FOOTER.md. Earlier decorative motion is quiet; media and functional controls remain. 8x Next is a placeholder, and unverified products have no links. The later v2 point morph was also rejected. Current v3 is a layered solid SVG turn and masked, stationary halftone reveal based on design/footer-layered-v3. The 1920×1080 hero now shows the full portrait frame. This implementation awaits user visual review.
Latest feedback supersedes the pending-review sentence above: the user likes the final dotted mark and responsive result. Preserve that endpoint. The refined motion now lifts three shallow layers, turns the complete mark, compresses the layers, prints the dots and holds the final frame over the last 17% of travel. This choreography awaits review. The hero orange circle is smaller and inset behind faces; fade the whole composition so orange cannot bleed through the clothing. Keep this fix across mobile and desktop.
Keep the original serif SVG mark. Studio montage appears ONLY in the hero. Use real original-site clips afterward; no fake metrics or generated videos. No new full-screen colour scenes, loading intro or scroll hijacking.
One native-scroll reading interval, fixed media geometries, restrained local focus changes. Support reduced motion, mobile, keyboard, playback errors and manual controls. No WebGL/animation framework needed.
Port3091 belongs to this app; do not stop unrelated ports3000/3088/3090. Work serially with one review browser. No delegation requested. Do not deploy without user request.
Before new Next APIs, consult node_modules/next/dist/docs. Use npm run build and targeted Playwright checks for meaningful behaviour. No unrequested dependency upgrades.
